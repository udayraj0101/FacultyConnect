// Single source of truth for the Faculty portal visual identity.
// Content-level accents (badge colors, type chips) live at the component that
// uses them — only brand-level tokens belong here.

export const FACULTY_GRADIENT = 'linear-gradient(135deg, #6C5CE7 0%, #A29BFE 100%)';
export const FACULTY_SHADOW = '0 20px 40px rgba(108, 92, 231, 0.15)';

// Optional secondary accent gradient — reserved for admin surfaces (navy).
export const ADMIN_GRADIENT = 'linear-gradient(135deg, #1A237E 0%, #4C4FE0 100%)';
